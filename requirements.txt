# Training / data-generation / reproduction environment (paper Section IV).
# Python 3.13. Pins the stack used to generate the dataset, train the model,
# and produce the figures. The Flask dashboard has its own pins in dashboard/requirements.txt.
torch==2.10.0
pandapower==3.4.0
numpy==2.3.5
scikit-learn==1.8.0
scipy==1.16.3
matplotlib==3.10.8
