# Pinned to the versions the reported results were produced with.
# pandapower 3.4.0 in particular: the AC oracle behind every label.
torch==2.10.0
pandapower==3.4.0
numpy==2.3.5
scipy==1.16.3
scikit-learn==1.8.0
matplotlib==3.10.8
flask==3.1.3
